# Week 1 — Python basics & first API call

Open `week01_python_basics_log_analyser.ipynb` (includes Session 2 cells at the bottom for your first Claude API call).

```text
pip install -r requirements.txt
```

Set `ANTHROPIC_API_KEY` before running the API cells.

The notebook creates `sample.log` in the working directory when you run the file I/O section.
